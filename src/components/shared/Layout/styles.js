import styled from "@emotion/styled";

export const StyledLayout = styled.div`
  display: grid;
  grid-auto-rows: max-content;
  width: 100vw;
  height: 100vh;
  overflow-y: auto;
  overflow-x: hidden;
  position: relative;
  z-index: 0;

`;
